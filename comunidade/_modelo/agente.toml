# TODO — one sentence saying what this agent answers and why it hurts.
#
# (The TOML keys below are still Portuguese — translating the spec schema is its
#  own step.)
#
# Compile with:  python -m forja comunidade/your-operation-name/agente.toml
# The forge has eight refusals, and all of them fail closed — if your spec asks
# for network without declaring a domain, or write without declaring a path, it
# does not compile, and the refusal comes with the fix written out. Run it
# before opening the PR.

[agente]
slug = "TODO-short-agent-name"
nome = "TODO — Readable name"
uma_frase = """
TODO — what it reads and returns, in one objective sentence. Always with "the file and line of \
every claim" at the end, where it applies — it is the house standard.\
"""
usar_quando = [
  "TODO — a concrete situation in which someone would call this agent",
]
nunca_usar = [
  "TODO — at least one case that looks obvious and is NOT for this agent; name the right agent if there is one on the shelf",
]

[fronteira]
# List only the tools this agent NEEDS. No `tools:` in today's harnesses means
# ALL of them — declaring too few is safer than forgetting to declare.
ferramentas = ["Read", "Grep", "Glob"]
toca_alvo = false

[prova]
lacunas = [
  "TODO — at least two. What this agent reads but cannot interpret; what it never sees because it never became a file; etc.",
]

[[prova.golden]]
pergunta = "TODO — a real question, with a concrete case (not generic: 'a file X does Y, what happens?')"
esperado = """
TODO — the answer a competent human would give, citing the real mechanism (not a manual-style \
explanation). Finish by explaining THE WHY of the behavior, not just the what.\
"""
derivado_de = "comunidade/your-operation-name/sondas.py — TODO: which probe or rule holds up this answer"
